version: "3"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./src:/src
    links:
      - mongodb
  mongodb:
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=interview-nodejs-db